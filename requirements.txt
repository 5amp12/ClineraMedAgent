numpy~=1.23.5
# app/ uses pydantic v2 APIs (model_validate, model_dump(mode="json")); the old ~=1.10 pin was
# wrong and only worked because the dev env happened to have v2 installed.
pydantic>=2.7
requests~=2.28.1
tqdm~=4.65.0
pyyaml~=6.0
jsonlines~=3.1.0
aiohttp~=3.8.4
uvicorn~=0.22.0
fastapi~=0.101.1
urllib3~=1.26.15
networkx~=2.8.4
fschat~=0.2.31
accelerate~=0.23.0
transformers~=4.34.0

# app/ (the live Clinera pipeline) — separate from the src/ benchmark harness above.
langgraph>=0.2
openai>=1.40
python-dotenv>=1.0
